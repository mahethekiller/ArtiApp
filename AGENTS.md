# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# API Data Fetching Requirement

All data displayed in the app should be fetched from the backend API endpoints (configured in `src/config.ts`) using `apiService`, rather than relying solely on local mock data fallback. Make sure that components are integrated properly with actual API responses.
